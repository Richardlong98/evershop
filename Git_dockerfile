FROM node:18

# Tạo thư mục làm việc
WORKDIR /usr/src/app

# Copy package.json trước để tối ưu cache
COPY package*.json ./

# Cài đặt dependencies
RUN npm install

# Copy toàn bộ code
COPY . .

# Build
RUN npm run build

# Expose port
EXPOSE 3000

# Start app
CMD ["npm", "run", "start"]
